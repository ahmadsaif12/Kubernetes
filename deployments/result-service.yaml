apiVersion : v1
kind: Service
metadata :
  name : result
  labels :
    app : result

spec :
  type : NodePort
  ports :
    - port : 80
      port: 8081
      targetPort : 80
      nodePort: 30081
      name : "result-service"
  selector :
    app : result
 