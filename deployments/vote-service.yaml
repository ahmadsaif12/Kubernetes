apiVersion : v1
kind: Service
metadata :
  name : vote
  labels :
    app : vote

spec :
  type : NodePort
  ports :
    - port : 80
      port: 8080
      targetPort : 80
      nodePort: 30080
      name : "vote-service"
  selector :
    app : vote
 